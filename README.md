# Test Task For IPONWEB

To install run `git clone git@github.com:alexartwww/test-iponweb.git ./test-iponweb`. Then run `composer install` in project directory.

To run tests use `phpunit ./src/Tests/UnitTests/`.
